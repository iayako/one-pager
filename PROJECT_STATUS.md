# Статус проекта: калькулятор себестоимости авто

Одностраничный калькулятор расчёта стоимости ввоза автомобиля из Японии в РФ по маршруту и правилам из **ТЕХЗАДАНИЕ калькулятор.pdf** (эталонные цифры — пример в ТЗ и скрин **Расчет себестоимости new 2.png**).

**Последнее обновление:** 2026-04-27 (демо-каталог авто: SQLite `car_selector.db`, API `api/vehicle_images.php`, карточки и каскадный выбор на лендинге; картинки — Wikipedia + заглушка; см. [CAR_SELECTOR_PLAN_STATUS.md](CAR_SELECTOR_PLAN_STATUS.md))

---

## Сделано

- [x] Документация расчёта: **[CALCULATION.md](CALCULATION.md)** — пошаговое описание формул, таблица комиссии аукциона, округления, отличия от полного ТЗ.
- [x] Просмотрено техническое задание (PDF): блоки инвойса в ¥, переводы валют, инвойс в ₽ с комиссией АТБ, таможня, итог с лабораторией.
- [x] Сверена логика примера из ТЗ со скриншотом Excel (итоговые суммы Train/Track совпадают).
- [x] Зафиксирован порядок реализации: курсы → инвойс ¥ → инвойс ₽ → таможня → итог.
- [x] Статический стек: `index.html`, `styles.css`, `app.js` (импортирует **`calc-core.js`** — вся формула в одном месте), форма, кнопка «Рассчитать», два блока результатов Train / Track.
- [x] Таблица доп. комиссии аукциона; выбор FOB (90–110 тыс. ¥); расчёт ¥-инвойса (Япония + $ доставка + перевод ₽ в ¥); оплата в банк с комиссией 1,5% и минимумом 10 000 ₽; итог с полями таможни и лаборатории.
- [x] Кнопка «Заполнить пример из ТЗ» — эталонные цифры из документа.
- [x] **Курсы валют** — компактная панель **над** блоком авто; далее **цена в ¥**, **возраст**, **тип двигателя**; **объём см³** и **мощность л.с.** — ниже после базовых полей; **«Рассчитать»** появляется, когда заполнены авто и все курсы. Сметовые блоки «Япония», «До Монголии», «Рубли в инвойсе» в UI убраны; для расчёта по ТЗ подставляются **скрытые** значения (FOB, vanning, $ Train/Track, ₽ в инвойсе, лаборатория). Таможня считается по формулам ТЗ.
- [x] Добавлено окно выбора аукциона со списком; выбранный аукцион автоматически задаёт `FOB` (90/100/110 тыс. ¥) для расчёта.
- [x] Проект выложен на VPS: домен `uus-avto.ru` (и `www`) привязан, HTTPS включён, сайт доступен в проде.
- [x] Настроен автодеплой из GitHub (`push` в `main`): `rsync` на `/var/www/one-pager` (секреты `config.php` / `telegram_config.php` и каталог `api/cache/` на приёмнике **не** затираются), после выкладки — права на кэш и перезапуск unit Telegram-бота.
- [x] Проверен backend на VPS: `api/health.php` отвечает, PHP исполняется, расширения `pdo_mysql`/`mysqli` включены.
- [x] Выполнено базовое SSH-hardening сервера: вход по паролю отключён, root SSH disabled, вход по ключам под пользователем `deploy`.
- [x] Подготовлены backend-файлы для БД: `api/db_ping.php`, `api/config.example.php`, локальный `api/config.php` (боевой вариант будет только на сервере, файл добавлен в `.gitignore`).
- [x] Подключение к MySQL подтверждено (успешный `db_ping`).
- [x] **Курс € / ₽ (ЦБ РФ):** `api/cbr_eur.php` — официальный дневной XML; без `date_req` подтягивается та же актуальная таблица, что на сайте ЦБ; кнопка **«ЦБ»** у поля «₽ за 1 €»; подпись с датой курса. Устойчивость к средам без CA / без ext-curl (системный `curl`, повтор при SSL).
- [x] **Курс ¥ / $ (Khan Bank):** `api/khan_rates.php` — JSON `https://www.khanbank.com/api/back/rates` (как на сайте банка); в поле «¥ за 1 $» подставляется `sellRate(USD) / buyRate(JPY)` в ₮; кнопка **«Khan»**; строка-пояснение с датой и курсами в ₮. Те же запасные пути HTTPS, что и для ЦБ.
- [x] **Снимок курсов по cron:** `api/refresh_rates_cache.php` (CLI) тянет Khan + АТБ + ЦБ и пишет `api/cache/rates_snapshot.json`; `api/rates_snapshot.php` отдаёт снимок фронту; при сбоях/устаревании снимка фронт ходит в живые API.
- [x] **Логирование расчётов в БД:** `api/save_calculation.php` принимает POST с `inputs` / `outputs`; фронт вызывает после каждого успешного `render` (ошибки игнорируются). Схема таблицы — `api/schema.sql` (`calculation_log`). На сервере таблицу нужно создать вручную, если ещё не выполнен SQL.
- [x] **Заявки с сайта:** `api/save_lead.php` + таблица `lead_request` (расширение полей — `api/migration_lead_client_fields.sql`); форма в `index.html` отправляет контакты и привязку к последнему расчёту.
- [x] **Telegram-бот (CLI, systemd):** `api/telegram_bot.php` — заявки `/leads`, карточка заявки с кнопкой связанного расчёта, `/id`, `/last`; токен в `api/telegram_config.php` (в Git не коммитится).
- [x] **Демо каталога автомобилей (встраивается в калькулятор):** локальная SQLite `car_selector.db` (таблицы `vehicles`, `vehicle_images` — см. `car_selector_schema.sql`); `GET api/vehicle_images.php` отдаёт список с URL картинки; опционально `?refresh=1` подставляет отсутствующие изображения через Wikipedia API. На фронте — быстрый поиск по пресетам, каскад марка → модель → год, сетка карточек с фото (`app.js`). Пакетное заполнение/обновление URL картинок: `scripts/refresh_vehicle_images.py` (повторы при сетевых сбоях).

---

## Нужно сделать

### Инфраструктура

- [x] Стек: статический `HTML/CSS/JS`.
- [x] Git: репозиторий и remote (при необходимости — донастроить `.gitignore`).
- [x] Прод-окружение: сайт на VPS + SSL (Let's Encrypt), домен `uus-avto.ru`.
- [x] CI/CD: GitHub Actions деплой на VPS по SSH (`VPS_SSH_KEY`) и `rsync`.
- [x] Базовый backend на VPS (PHP API в `/api`).
- [x] MySQL доступна и подключается из backend.
- [ ] Ротация CI SSH-ключа (`VPS_SSH_KEY`) после публикации ключа в скриншоте.

### Данные и константы

- [x] Таблица доп. комиссии аукциона от цены (¥).
- [x] Справочник «аукцион → FOB» (90/100/110 тыс. ¥): окно выбора со списком, подстановка FOB в форму.
- [x] Vanning, суммы $ Train/Track по умолчанию; ₽ для перевода в инвойс (по умолчанию таможенная очистка).
- [ ] Фиксированные суммы из ТЗ: отдельные $-строки (контейнер, документы, граница, автовоз) как опциональные поля.
- [x] Таблицы таможенных сборов по диапазонам таможенной стоимости (₽).
- [x] Правила пошлины по возрасту авто и ценовым коридорам в € + минимумы €/см³ (как в ТЗ).
- [x] Расчёт утилизационного сбора (до 160 л.с. по ТЗ; свыше — оценка в коде).

### Логика расчёта

- [x] Курсы: ввод ¥/$, ₽/$, ₽/¥, ₽/€ (€ — пошлина до 3 лет и €/см³ для остальных возрастов).
- [x] Сумма инвойса в ¥ для Train и Track; ₽→¥ для строки в инвойсе по формуле ТЗ.
- [x] Инвойс в ₽: база ¥×₽/¥ + комиссия 1,5% с минимумом 10 000 ₽.
- [x] Автоматический расчёт таможни (сборы, пошлина, утилизация) по формулам ТЗ; эталон **506 719 ₽** на примере из кнопки.
- [x] Итог: инвойс ₽ + таможня + лаборатория; два финала Train/Track (при эталонных курсах совпадают с ТЗ: **1 846 437 / 1 872 480 ₽**).

### Backend и БД

- [x] API сохранения расчёта: `api/save_calculation.php` + таблица `calculation_log` (см. `api/schema.sql`); вызов с фронта при каждом пересчёте.
- [ ] На проде убедиться, что `schema.sql` применён и запись в лог не падает с 503.
- [x] Просмотр заявок и расчётов в Telegram-боте (см. выше). Отдельной веб-админки / выгрузки логов нет.

### Интерфейс

- [x] Одна страница: компактные курсы сверху, блок «Автомобиль» (доп. поля ниже после базовых); результаты Train/Track в двух колонках.
- [x] Подписи и подсказки; сроки доставки в карточках Train / Track.
- [x] Кнопки подстановки курсов **ЦБ** (€) и **Khan** (¥/$) с поясняющими подписями.

### Опционально (после MVP)

- [x] Подстановка **₽/$ с сайта АТБ** ([atb.su/services/exchange](https://www.atb.su/services/exchange/?changecity=yes)) для города Улан-Удэ — PHP-эндпоинт `api/atb_usd_ulanude.php` (Bitrix `setCity` + чтение коэффициентов конвертера), кнопка **«АТБ»** рядом с полем «₽ за 1 $»; курс подтягивается при клике. Подстановка ₽/¥ пока остаётся ручной.
- [ ] Сохранение/экспорт расчёта (PDF/печать/копирование).
- [ ] Тесты на эталонные числа из ТЗ.

---

## Эталон для проверки (из ТЗ)

| Показатель              | Train        | Track        |
|-------------------------|-------------|-------------|
| Инвойс, ¥               | 2 416 514   | 2 464 934   |
| Инвойс, ₽ (с комиссией) | 1 299 718   | 1 325 761   |
| Всего с доставкой, ₽    | 1 846 437   | 1 872 480   |

Таможенные платежи (пример): **506 719 ₽**. Лаборатория: **40 000 ₽**.

---

## Файлы в папке

| Файл | Назначение |
|------|------------|
| `ТЕХЗАДАНИЕ калькулятор.pdf` | Исходное ТЗ |
| `ТЕХЗАДАНИЕ калькулятор.docx` | ТЗ в Word |
| `Расчет себестоимости new 2.png` | Скрин эталонной таблицы Excel |
| `PROJECT_STATUS.md` | Этот файл — статус и чеклист |
| `index.html` | Разметка калькулятора |
| `styles.css` | Стили |
| `app.js` | UI, опрос курсов, пример из ТЗ, кнопки Khan / ЦБ / АТБ, отправка лога и заявок |
| `calc-core.js` | Чистая логика расчёта (`computeCalculation` и формулы), импортируется в `app.js` |
| [CALCULATION.md](CALCULATION.md) | Подробное описание логики расчёта (формулы, эталон, ограничения) |
| `api/cbr_eur.php` | Курс €/₽ с сайта ЦБ |
| `api/khan_rates.php` | Пересчёт ¥/$ по курсам Khan Bank (`api/back/rates`) |
| `api/atb_usd_ulanude.php` | Курс ₽/$ по АТБ для города Улан-Удэ (как на странице обмена валют) |
| `api/rates_snapshot.php` | JSON последнего снимка курсов из `api/cache/rates_snapshot.json` |
| `api/refresh_rates_cache.php` | CLI: обновить снимок (cron), аргумент — базовый URL сайта |
| `api/save_calculation.php` | Сохранение снимка расчёта в MySQL |
| `api/save_lead.php` | Приём заявки с лендинга (контакты + привязка к расчёту) |
| `api/telegram_bot.php` | Long polling: заявки и просмотр `calculation_log` в Telegram |
| `api/schema.sql` | Таблица `calculation_log`; заявки — в миграции `migration_lead_client_fields.sql` |
| `scripts/fix-cache-perms.sh` | После деплоя: права на `api/cache` для `www-data` (без sudo) |
| `api/health.php`, `api/db_ping.php` | Диагностика PHP и БД на хостинге |
| `car_selector.db`, `car_selector_schema.sql` | Локальная SQLite: демо-авто и привязанные картинки |
| `api/vehicle_images.php` | JSON каталог авто для пресетов и карточек; опционально дозаполнение картинок |
| `scripts/refresh_vehicle_images.py` | CLI: подобрать картинки по make/model/year (Wikipedia) и записать в `vehicle_images` |

---

## Заметки

- Исходники: `index.html`, `styles.css`, `app.js`. Для PHP-эндпоинтов — локально `php -S localhost:8080` из корня репозитория (не `file://`).
- Подробности формул и округлений — в **[CALCULATION.md](CALCULATION.md)**.
- Кратко: база в ₽ = `round(¥ × ₽/¥)`, комиссия = `max(floor(база × 1,5%), 10 000)` — как в примере ТЗ.
- Источник URL для Khan Bank: в открытом репозитории [mongolian-bank-exchange-rate](https://github.com/btseee/mongolian-bank-exchange-rate) (`KHANBANK_URI` в конфиге); это не официальная документация банка.
